#include "main.h"
streat concatennates two strings
@dest: copy to
@src: copy from
• Return: pointer to dest
*/
char_strcat(char *dest, char *src)
{
int 1;
int i;
while (dest[1] != '\0')
(
}
j= 0;
while (src[j] != '\0¹)
{
dest[1]= src[j];
}
dest[i] '\0';
return
